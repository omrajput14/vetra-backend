package app.vetra.ai.workflow.clinical.model.action;

/** Target actor responsible for executing or initiating the action. */
public enum ActionActor {
  FARMER,
  CAREGIVER,
  VETERINARIAN,
  LABORATORY,
  SYSTEM
}
