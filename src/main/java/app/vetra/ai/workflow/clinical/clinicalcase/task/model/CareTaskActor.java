package app.vetra.ai.workflow.clinical.clinicalcase.task.model;

/** Target responsible actor for clinical care task execution. */
public enum CareTaskActor {
  VETERINARIAN,
  CAREGIVER,
  LABORATORY,
  SYSTEM,
  REFERRAL_PROVIDER
}
