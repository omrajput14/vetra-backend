package app.vetra.ai.workflow.clinical.model.action;

/** Operational action type classification. */
public enum ActionType {
  VETERINARY_REFERRAL,
  IMMEDIATE_CARE,
  ISOLATION,
  MEDICATION,
  MONITORING,
  DIAGNOSTIC_TEST,
  FOLLOW_UP,
  PREVENTIVE_CARE,
  OWNER_NOTIFICATION
}
